steps:
  # Step 1: Build Docker image with training code and dependencies
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'us-docker.pkg.dev/udemy-mlops/mlops-repo/bank-campaign-model:latest'
      - '.'

  # Step 2: Push image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'us-docker.pkg.dev/udemy-mlops/mlops-repo/bank-campaign-model:latest'

  # Step 3: Run unit tests inside the container
  - name: 'us-docker.pkg.dev/udemy-mlops/mlops-repo/bank-campaign-model:latest'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        python -m pytest test_training.py -v

  # Step 4: Clone the model training repository
  - name: 'gcr.io/cloud-builders/git'
    args:
      - 'clone'
      - 'https://github.com/sidoncloud/model-training-repo.git'

  # Step 5: Copy updated training script to Composer DAGs bucket
  - name: 'gcr.io/cloud-builders/gsutil'
    args:
      - 'cp'
      - 'model-training-repo/bank_campaign_model_training.py'
      - 'gs://us-central1-mlops-airflow-faa6f9bc-bucket/dags'

images:
  - 'us-docker.pkg.dev/udemy-mlops/mlops-repo/bank-campaign-model:latest'

options:
  logging: CLOUD_LOGGING_ONLY
